import java.util.List;

@Herbivore
public class Mouse extends Animal{
    public Mouse(int x, int y, int number, int energy, boolean isAlive, String gender, Type type) {
        super(x, y, number, energy, isAlive, gender, type);
    }
}
