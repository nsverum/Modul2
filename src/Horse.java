import java.util.List;
import java.util.Map;

@Herbivore
public class Horse extends Animal {

    public Horse(int x, int y, int number, int energy, boolean isAlive, Type type) {
        super(x, y, number, energy, isAlive,  type);
    }
}
